swagger: '2.0'
info:
  description: This file contains REST API specification for payment project. The file is autogenerated from the swagger definition.
  version: 0.1.38
  title: Payment Backend API
host: localhost:8080
basePath: /api
schemes:
  - http
  - https
consumes:
  - application/json
produces:
  - application/json
paths:
  /signUp:
    post:
      tags:
        - payment
      summary: signup a user
      operationId: create_user
      responses:
        '200':
          description: A successful response.
          schema:
            $ref: '#/definitions/apiUserCreationResponse'
        default:
          description: ''
          schema:
            $ref: '#/definitions/apiStatus'
      x-swagger-router-controller: manager.swagger_server.controllers.config_controller
definitions:
  apiStatus:
    type: object
    properties:
      msg:
        type: string
      code:
        type: integer
        format: int32
      data:
        type: object
        properties:
          log:
            type: string
          success:
            type: boolean
    example:
      msg: string
      code: 0
      data: string
  apiUserCreationResponse:
    type: object
    properties:
      msg:
        type: string
      code:
        type: integer
        format: int32
      data:
        type: object
        properties:
          id:
            type: integer
          firstName:
            type: string
          lastName:
            type: string
          email:
            type: string
          phone:
            type: string
          createdAt:
            type: integer
